{
  "name": "vite-vue-electron-demo",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "main": "main.js",
  "scripts": {
    "start": "start cmd /k pnpm vite:dev && pnpm elec:dev",
    "elec:dev": "electron .",
    "vite:dev": "pnpm -r vite:dev",
    "build:hot": "vue-tsc --noEmit && vite build --watch",
    "build": "vue-tsc --noEmit && vite build"
  },
  "devDependencies": {
    "electron": "^20.1.3"
  }
}